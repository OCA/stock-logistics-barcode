In daily picking operations, the label printing options are usally the same
for the same kind of operations.

Putting some default configurations would help users to not select the same options
again and again.
