<?xml version="1.0" encoding="utf-8"?>
<!--
    Copyright 2016 Eficent Business and IT Consulting Services, S.L.
    <http://www.eficent.com>
    License AGPL-3.0 or later (http://www.gnu.org/licenses/agpl).
-->
<odoo>

    <template id="hardware_select" name="header">
        <t t-call="web.layout">
            <div id="wrap" class="oe_structure oe_empty" data-role="page">
                <div data-role="main" class="ui-content">
                    <div class="col-xs-12 text-center">
                        <p t-if="code == 'L'">
                            <center>
                                <h3>Please, Select your hardware:</h3>
                            </center>
                            <div data-role="controlgroup" data-type="vertical">
                                <t t-foreach="result" t-as="line">
                                    <a id="btn-go" t-attf-href="/stock_scanner_web/{{line}}" class="ui-btn">
                                        <span t-esc="line"/>
                                    </a>
                                </t>
                            </div>
                        </p>
                        <p t-if="code == 'N'">
                            <h3>
                                <span t-esc="result"/>
                            </h3>
                            <a id="btn-back" href="#" data-rel="back" class="ui-btn ui-btn-b">
                                Back to Odoo
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="error_message" name="header">
        <t t-call="web.layout">
            <div id="wrap" class="oe_structure oe_empty" data-role="page">
                <div data-role="main" class="ui-content">
                    <div class="col-xs-12 text-center">
                        <h3>
                            <span t-esc="result"/>
                        </h3>
                        <p t-if="code == 'E'">
                            <a id="btn-go" t-attf-href="/stock_scanner_web/" class="ui-btn">
                                Back
                            </a>
                        </p>
                    </div>
                </div>
            </div>
        </t>
    </template>

    <template id="scanner_call" name="header">
        <head>
            <link rel='stylesheet' href='/web/static/lib/bootstrap/css/bootstrap.css'/>
        </head>
        <t t-call="web.layout">
            <div id="wrap">
                <div class="container">
                  <t t-if="result.get('header')">
                      <center>
                          <h2 t-esc="result.get('header')"/>
                      </center>
                  </t>
                    <p t-if="code == 'L'">
                        <div class="row">
                            <t t-foreach="result.get('lines')" t-as="line">
                                <div class="col-sm-12 col-md-6 col-lg-4">
                                    <a class="btn btn-info" role="button" t-attf-href="{{line.get('href')}}">
                                        <span t-esc="line.get('label')"/>
                                    </a>
                                </div>
                            </t>
                        </div>
                    </p>
                    <p t-if="code == 'M'">
                        <t t-foreach="result.get('lines')" t-as="line">
                            <p t-esc="line"/>
                        </t>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/action/0" class="ui-btn">
                            Go
                        </a>
                    </p>
                    <p t-if="code == 'E'">
                        <t t-foreach="result.get('lines')" t-as="line">
                            <p t-esc="line"/>
                        </t>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/back" class="ui-btn">
                            Back
                        </a>
                    </p>
                    <p t-if="code == 'F'">
                        <t t-foreach="result.get('lines')" t-as="line">
                            <p t-esc="line"/>
                        </t>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/Menu" class="ui-btn">
                            Go to menu
                        </a>
                    </p>
                    <p t-if="code == 'C'">
                        <t t-foreach="result.get('lines')" t-as="line">
                            <p t-esc="line"/>
                        </t>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/action?message=True" class="ui-btn">
                            Yes
                        </a>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/action?message=False" class="ui-btn">
                            No
                        </a>
                    </p>
                    <p t-if="code in ['N','Q']">
                        <t t-foreach="result.get('lines')" t-as="line">
                            <p t-esc="line"/>
                        </t>
                        <form method="get" t-att-action="'/stock_scanner_web/%s/%s/action' % (terminal_number, step)">
                            <input type="text" name="message" class="search-query form-control" autofocus="autofocus"/>
                        </form>
                        <button class="ui-btn ui-icon-carat-r ui-alt-icon ui-btn-icon-left ui-nodisc-icon" form="my_form">Submit
                        </button>
                    </p>
                    <p t-if="code == 'T'">
                        <t t-foreach="result.get('lines')" t-as="line">
                            <p t-esc="line"/>
                        </t>

                        <form method="get" t-att-action="'/stock_scanner_web/%s/%s/action' % (terminal_number, step)" class="search_class form-inline">
                            <div class="input-group">
                                <input type="text" name="message" class="search-query form-control" autofocus="autofocus"/>
                                <span class="input-group-btn">
                                    <button type="submit" class="ui-btn-b"><i class="fa fa-go"/>Submit</button>
                                </span>
                            </div>
                        </form>
                    </p>
                    <p t-if="code == 'R'">
                        <h3>
                            <t t-foreach="result" t-as="line">
                                <span t-esc="line"/>
                            </t>
                        </h3>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/Menu" class="ui-btn">
                            Go to menu
                        </a>
                    </p>
                    <p t-if="code == 'U'">
                        <h3>Please, contact you administrator</h3>
                        <a id="btn-go" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/back" class="ui-btn">
                            Go to menu
                        </a>
                    </p>
                    <p t-if="code == 'W'">
                        <t t-call="{{result}}"/>
                    </p>
                    <p t-if="action is not 'reset'">
                        <a id="btn-home" t-attf-href="/stock_scanner_web/{{terminal_number}}/{{step}}/?action=reset" class="ui-btn ui-mini">
                            Main Menu
                        </a>
                    </p>
                </div>
            </div>
        </t>
    </template>
</odoo>
